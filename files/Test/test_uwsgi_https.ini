[uwsgi]
base = /srv/Test
chdir = %(base)
virtualenv = %(base)/venv
plugins = python3
mount = /=app:app
manage-script-name = true

env = LANG=en_US.utf8

socket = /srv/sockets/%n.sock
chmod-socket = 666

logto = %(base)/data/%n.log
